# 2026-04-27

## 浅色主题修正与新增站点独立页面

- 修复了 popup 浅色模式未真正覆盖关键容器的问题，把卡片、右键菜单、编辑面板、输入框、遮罩等区域统一改为走主题 token；根因不是 `colorMode` 状态失效，而是关键容器仍保留了硬编码深色背景。
- 恢复了 popup 顶部与设置区中文文案：`新增 / 配置 / 同步`，并把设置项文案从“卡片大小”调整为“卡片”。
- 删除了 popup 内旧的 `addSiteWizard` DOM 和脚本逻辑，新增按钮现在会打开独立页面 [add-site.html](../add-site.html)。
- 新增了共享模块 [site-config-shared.js](../site-config-shared.js)，把字段提取、错误分类、可选除法运算、自定义站点构建等纯逻辑抽离出来，供 popup 与 add-site 复用。
- 新增了独立页面 [add-site.html](../add-site.html) 与 [add-site.js](../add-site.js)，实现了第一版新增站点流程：
  - 基础信息：站点名称 + 接口地址 + 监控字段
  - 自动抓取测试
  - 鉴权失败时按需补录 `authorization` / `token` / `new-api-user`
  - 抓取成功后进入运算步骤
  - 支持“不需要”直接跳过运算
  - 支持除法配置与实时预览
  - 只有手动点击“完成”才真正写入 `chrome.storage.local`
- 补齐了两个关键细节：
  - 鉴权失败后再次点击“下一步”时，允许连同 URL / 字段路径一起修正后重试，而不只是补 header。
  - 非法除数（例如 `0`）不再静默回退为“不运算”，会明确提示用户除数无效。
- popup 侧补充了外部 `siteConfigs` 变更的应用逻辑，便于独立新增页面保存后让 popup 运行态吸收新的自定义站点配置。

## 新增/更新测试

- 更新 [task-popup-ui.test.js](../task-popup-ui.test.js)
- 更新 [task-site-config-state.test.js](../task-site-config-state.test.js)
- 新增 [task-add-site-ui.test.js](../task-add-site-ui.test.js)
- 新增 [task-add-site-flow.test.js](../task-add-site-flow.test.js)
- 调整 [task-click-refresh.test.js](../task-click-refresh.test.js)、[task-compact-layout.test.js](../task-compact-layout.test.js)、[task-yybb-diagnostics.test.js](../task-yybb-diagnostics.test.js) 的 sandbox，使其与共享脚本加载顺序一致

## 回归结果

已通过：

- `node "/e/vscode/009_chromeExt/task-popup-ui.test.js"`
- `node "/e/vscode/009_chromeExt/task-site-config-state.test.js"`
- `node "/e/vscode/009_chromeExt/task-click-refresh.test.js"`
- `node "/e/vscode/009_chromeExt/task-compact-layout.test.js"`
- `node "/e/vscode/009_chromeExt/task-yybb-diagnostics.test.js"`
- `node "/e/vscode/009_chromeExt/task-add-site-ui.test.js"`
- `node "/e/vscode/009_chromeExt/task-add-site-flow.test.js"`
- `node --check "/e/vscode/009_chromeExt/popup.js"`
- `node --check "/e/vscode/009_chromeExt/add-site.js"`

## 站点管理页整合 popup 右键能力

- popup 顶部入口从“新增”改成了“站点”，并彻底移除了 popup 内的站点级右键菜单、token 编辑器、站点名称编辑器相关 DOM 与脚本逻辑，让 popup 回到轻量看板职责。
- 将 [add-site.html](../add-site.html) 从单一“新增站点页”扩展为“站点管理页”，新增左侧功能切换：`新增站点` / `修改站点`。
- `新增站点` 模式继续保留原有流程：基础信息 → 自动测试抓取 → 按需鉴权 → 可选除法运算 → 手动点击“完成”后落盘。
- `修改站点` 模式新增了统一站点管理能力：
  - 读取当前站点列表
  - 选中后回填站点名称、接口 URL、监控字段、headers/token、divideBy
  - 支持“测试配置”先验证再保存
  - 支持保存修改
  - 支持删除站点
- 删除语义保持与原协议一致：
  - 内置站点写入 `removed: true`
  - 自定义站点直接从持久化列表移除
- popup 保留 `siteConfigs` 外部变更吸收逻辑，因此站点管理页保存或删除后，popup 运行态可以通过 storage 监听立即反映结果。

## 本轮新增/更新测试与验证

- 更新 [task-popup-ui.test.js](../task-popup-ui.test.js)，锁定 popup 顶部文案与“无右键编辑 UI”边界。
- 更新 [task-add-site-ui.test.js](../task-add-site-ui.test.js)，锁定站点管理页双模式结构。
- 更新 [task-add-site-flow.test.js](../task-add-site-flow.test.js)，覆盖修改模式的列表、回填、测试、保存、删除链路。
- 更新 [task-site-config-state.test.js](../task-site-config-state.test.js)，覆盖内置/自定义站点编辑后的 popup 吸收行为与 removed 语义。
- 本轮已验证通过：
  - `node "/e/vscode/009_chromeExt/task-popup-ui.test.js"`
  - `node "/e/vscode/009_chromeExt/task-site-config-state.test.js"`
  - `node "/e/vscode/009_chromeExt/task-click-refresh.test.js"`
  - `node "/e/vscode/009_chromeExt/task-compact-layout.test.js"`
  - `node "/e/vscode/009_chromeExt/task-yybb-diagnostics.test.js"`
  - `node "/e/vscode/009_chromeExt/task-add-site-ui.test.js"`
  - `node "/e/vscode/009_chromeExt/task-add-site-flow.test.js"`
  - `node --check "/e/vscode/009_chromeExt/popup.js"`
  - `node --check "/e/vscode/009_chromeExt/add-site.js"`

## 默认站点配置外置与敏感信息脱敏

目标：为公开发布做准备，去除主代码中的真实 token / authorization / new-api-user 值。

### 改动清单

- 新增 `site-config.public.json`：3 个默认站点的脱敏版配置，所有敏感 header 使用占位符（`YOUR_AUTHORIZATION`、`YOUR_NEW_API_USER`）。
- 新增 `site-config.archive.json`：当前 17 个站点的完整归档，保留真实值供导入测试，**不应打包到公开发布**。
- 改造 `site-config-shared.js`：
  - `DEFAULT_SITE_CONFIGS` 从 17 条硬编码配置改为空数组 `[]`。
  - 新增导出 `buildDefaultSiteConfigIds(defaultConfigs)` 供消费者按需构建 ID 集合。
  - 所有纯逻辑函数保持不变。
- 改造 `popup.js`：
  - 新增 `loadDefaultSiteConfigs()` 异步函数，通过 `fetch(chrome.runtime.getURL('site-config.public.json'))` 加载外置 JSON。
  - `CONFIG` 改为 `let CONFIG = []`，在 `init()` 首行 `await loadDefaultSiteConfigs()` 后再构建。
  - 所有 `DEFAULT_SITE_CONFIGS` / `DEFAULT_SITE_CONFIG_IDS` 引用替换为 `getDefaultSiteConfigs()` / `getDefaultSiteConfigIds()`。
- 改造 `add-site.js`：
  - 同样新增 `loadDefaultSiteConfigs()` 等函数，模块级启动 `_defaultConfigsPromise`。
  - 页面初始化改为 `_defaultConfigsPromise.then(() => ...)` 确保外置 JSON 先加载。
  - 所有静态默认配置引用替换为动态 getter。

### 测试更新

- `task-site-config-state.test.js`：新增断言验证 popup 从外置 JSON 加载、脱敏占位符正确、加载数量为 3。
- `task-add-site-flow.test.js`：新增 `await _defaultConfigsPromise` 和 fetchCalls 断言。
- `task-click-refresh.test.js`、`task-compact-layout.test.js`、`task-yybb-diagnostics.test.js`：沙箱统一补充 `chrome.runtime.getURL` 和 `site-config.public.json` 的 fetch mock，支持异步初始化。
- `task-compact-layout.test.js`：从同步 IIFE 改为 async IIFE。

### 敏感值清查

- `.js` 文件中搜索 `eyJhbGci`（JWT）、`b4996039`（Bearer token）、数字型 `new-api-user` 值：无残留。
- `site-config.public.json` 仅包含占位符。
- `site-config.archive.json` 保留真实值，需确保不进入公开发布包。

### 回归结果

全部 7 个测试通过：
- `task-site-config-state.test.js`
- `task-click-refresh.test.js`
- `task-compact-layout.test.js`
- `task-yybb-diagnostics.test.js`
- `task-popup-ui.test.js`
- `task-add-site-ui.test.js`
- `task-add-site-flow.test.js`

## 简短复盘

- 值得保留：先用测试锁定”先测试、后保存”和”popup 只做看板”这两个边界，再推进共享协议与页面迁移，能显著降低改坏旧行为的概率。
- 值得保留：外置 JSON 方案下，用 `fetch` mock + `__popupInitPromise` 让测试沙箱能完整模拟异步初始化链路，比硬编码默认配置时的测试覆盖面更真实。
- 可以优化：虽然 storage 同步已经能让 popup 立即吸收站点页改动，但后续如果继续增强体验，可以补一层更明确的”保存成功 / 已同步到看板”可视反馈，并做一次浏览器人工联调验收。
- 待确认：`site-config.archive.json` 是否需要加入 `.gitignore` 或发布打包排除策略，避免真实 token 误发布。

## 空白初始态与站点配置导入导出

目标：公开发布时不再自动显示真实或模板站点，让用户通过站点管理页自行导入/导出配置。

### 改动清单

- 将 `site-config.archive.json` 加入 `.gitignore`，避免真实归档配置误发布。
- 改造 [site-config-shared.js](../site-config-shared.js)：
  - 新增 `DEMO_SITE_CONFIGS`，提供 2 个仅用于引导操作的示例卡片。
  - 新增 `isDemoTask()`。
  - `normalizeSiteConfigs()` / `serializeSiteConfigs()` / runtime 构建链路保留 `isDemo` 标记。
- 改造 [popup.js](../popup.js)：
  - popup 不再自动加载 `site-config.public.json` 作为默认站点。
  - 首次且 storage 为空时注入 2 个 demo 卡片，并写入 `_demoInjected`，删除后不会反复出现。
  - demo 卡片刷新不发真实网络请求，批量刷新会跳过 demo。
  - 默认站点为空时清理历史残留 `boardData` / `manualOrder` / `groups`。
- 改造 [add-site.html](../add-site.html)：
  - 新增站点模式增加“从文件导入”按钮、JSON 文件 input 和导入结果提示区。
  - 修改站点模式增加“导出配置”按钮。
- 改造 [add-site.js](../add-site.js)：
  - `createSiteManagerController()` 新增 `importSiteConfigs()`，校验数组、归一化配置、同 id 覆盖、新 id 追加，并写入 `chrome.storage.local.siteConfigs`。
  - `createPageApp()` 新增 `importSiteConfigsFromText()`、文件导入处理与 `exportSiteConfigs()`。
  - 导出当前非 demo 站点为 `site-configs-YYYY-MM-DD.json`，导出的 JSON 可再导入。
  - 扩展 `__testExports`，暴露页面级导入导出测试需要的接口。

### 新增/更新测试

- 新增 [task-demo-cards.test.js](../task-demo-cards.test.js)，覆盖 demo 配置、首次注入和 demo 刷新跳过网络请求。
- 新增 [task-import-export.test.js](../task-import-export.test.js)，覆盖导入覆盖/追加、非法结构校验、页面文本导入、导出过滤 demo。
- 更新 [task-site-config-state.test.js](../task-site-config-state.test.js)，锁定 popup 不再自动 fetch 默认 JSON。
- 更新 [task-click-refresh.test.js](../task-click-refresh.test.js)、[task-compact-layout.test.js](../task-compact-layout.test.js)、[task-yybb-diagnostics.test.js](../task-yybb-diagnostics.test.js)，改为显式注入测试站点配置。
- 更新 [task-add-site-ui.test.js](../task-add-site-ui.test.js)、[task-add-site-flow.test.js](../task-add-site-flow.test.js)，覆盖导入/导出入口与控制器行为。

### 回归结果

已通过：

- `for f in /e/vscode/009_chromeExt/task-*.test.js; do node "$f" || exit 1; done`
- `node --check "/e/vscode/009_chromeExt/popup.js"`
- `node --check "/e/vscode/009_chromeExt/add-site.js"`
- `node --check "/e/vscode/009_chromeExt/site-config-shared.js"`

### 简短复盘

- 值得保留：这次先用红灯测试锁定“无默认站点”“demo 不请求网络”“导出过滤 demo”“导入落盘”这些产品边界，再改生产代码，迁移默认行为时更稳。
- 值得保留：把导入/导出都走已有 `normalizeSiteConfigs()` / `serializeSiteConfigs()` 协议，避免页面层另起一套 JSON 格式。

## 站点管理页两个交互 bug 修复

目标：修复 009 项目里两个现有交互问题——单张卡片点击后删除站点失败，以及新增站点在“运算”步骤手动输入公式时下方预览值不更新。

### 改动清单

- 改造 [add-site.js](../add-site.js)：
  - 修正 `readEditDraftFromInputs()`，编辑页读取公式时只使用 `editFormulaInput`，不再错误兜底到新增流程的 `formulaInput`。
  - 这样可以避免新增流程与编辑流程共用页面 DOM 时，编辑区残留字段值干扰新增流程的手动公式预览。
- 更新 [task-add-site-flow.test.js](../task-add-site-flow.test.js)：
  - 补充 `editFormulaInput` 的测试桩节点，模拟新增/编辑两个公式输入框同时存在的真实页面结构。
  - 新增回归断言：当 `editFormulaInput` 存在且带有其他值时，新增流程手动修改 `formulaInput` 仍应驱动 `previewValueText` 按新增输入框的值实时刷新。
  - 收紧单卡删除链路测试，显式经过修改模式入口后再执行删除，贴近用户点击单卡后删除的真实路径。

### 回归结果

已通过：

- `node "E:/vscode/009_chromeExt/task-add-site-flow.test.js"`

### 简短复盘

- 值得保留：这次问题表面上是“预览不刷新”，但根因是同页双表单共存时读错了输入源；先把失败场景压进测试，再改最小一行逻辑，定位会更稳。
- 可以优化：站点管理页把新增与编辑表单同时保留在一个文档里，后续如果类似问题继续出现，可以考虑把读值逻辑继续按视图拆分，而不是依赖跨表单兜底。

## 配置看板单卡编辑入口与编辑页公式实时预览修复

目标：修复用户从配置看板首页点击单卡后的真实编辑路径问题，并补齐编辑页公式链路，让“删除站点”和“公式预览”都能按用户直觉工作。

### 改动清单

- 改造 [add-site.js](../add-site.js)：
  - 在 `openSiteEdit()` 中显式调用 `setMode('edit')`，保证从首页点击单卡后真正切到“修改站点”面板，而不是停留在新增面板。
  - 补全 `createDomRefs()` 中缺失的 `editFormulaInput` 引用，修复编辑页公式输入框未接入脚本的问题。
  - 为 `createSiteManagerController()` 新增基于最近测试原值的 `recalculatePreview()`，复用现有公式计算逻辑，在编辑页不重复造一套计算器。
  - 新增 `syncEditFormulaPreview()`，当编辑页已经测试成功后，手动修改公式可立即刷新右侧“实时预览区”；若尚未测试，则明确提示先点“测试配置”获取原值。
- 更新 [task-add-site-flow.test.js](../task-add-site-flow.test.js)：
  - 锁定从首页点击单卡后必须显示编辑面板并能看到删除按钮。
  - 新增断言：已有公式站点进入编辑页后，点击“测试配置”应按公式更新右侧预览。
  - 新增失败后转绿的回归：编辑页测试成功后，手动修改 `editFormulaInput`，右侧预览必须实时变化。

### 回归结果

已通过：

- `node "E:/vscode/009_chromeExt/task-add-site-flow.test.js"`
- `node "E:/vscode/009_chromeExt/task-formula-calculation.test.js"`
- `node --check "E:/vscode/009_chromeExt/add-site.js"`

### 简短复盘

- 值得保留：这次两个表面问题——“看不到删除按钮”和“公式不运算”——最终都指向同一个事实：编辑态 UI 已存在，但脚本没有完整接上真实编辑路径。先按用户实际点击路线补失败测试，比凭代码猜 UI 状态更可靠。
- 值得保留：编辑页实时预览没有复制新增页的整套控制器，而是复用最近一次测试成功得到的原值去重算，改动小、风险低。
- 可以优化：当前编辑页仍要求“先测试配置，再输入公式才有实时预览”，这符合数据真实性，但后续如果要进一步提升体验，可以考虑把“最近成功抓取值”更明确地展示为编辑态缓存状态。

## 编辑页原值抓取状态隔离与紧凑布局修复

目标：修复编辑页中“公式输入不应污染原值抓取状态”的问题，并把编辑页基础字段/鉴权字段压缩到两行三列布局，减少页面高度。

### 改动清单

- 改造 [add-site.js](../add-site.js)：
  - 新增 `resetEditPreview()`，进入编辑页时统一重置左侧原值与右侧预览，避免把临时抓取结果当成配置回填。
  - 保持 `testEditSite()` 为唯一刷新左侧“抓取原值”的入口，公式输入链路不再负责改写原值区。
  - 保留编辑页公式只读写 `editFormulaInput`，新增页 `formulaInput` 不再被编辑回填污染。
- 改造 [add-site.html](../add-site.html)：
  - 编辑页基础字段改为复用三列 `create-form-grid`：站点名 / 接口地址 / 监控字段 同一行。
  - 编辑页鉴权字段改为 `auth-grid edit-auth-grid`：authorization / token / new-api-user 同一行，不再让 `new-api-user` 独占整行。
- 更新 [task-add-site-flow.test.js](../task-add-site-flow.test.js)：
  - 补充回归：编辑页点击“测试配置”后左侧原值出现，修改公式后再次测试时原值保持稳定、右侧预览按公式变化。
  - 补充回归：保存后重新进入编辑页，不回填临时原值；再次点击“测试配置”仍可重新抓取原值并按已保存公式出预览。
- 更新 [task-add-site-ui.test.js](../task-add-site-ui.test.js)：
  - 新增编辑页布局结构断言，锁定基础字段三列与鉴权字段三列同排。

### 回归结果

已通过：

- `node "E:/vscode/009_chromeExt/task-add-site-flow.test.js"`
- `node "E:/vscode/009_chromeExt/task-add-site-ui.test.js"`
- `node "E:/vscode/009_chromeExt/task-formula-calculation.test.js"`
- `node --check "E:/vscode/009_chromeExt/add-site.js"`

### 简短复盘

- 值得保留：把“测试配置刷新原值”和“公式只改预览”拆成两条明确职责后，问题判断会清晰很多，避免再次把临时预览态错误写回配置态。
- 值得保留：布局压缩用已有 grid class 组合实现，而不是额外发明新容器，改动小、回归面也更可控。
- 可以优化：编辑页目前仍要求用“测试配置”来刷新原值，这在正确性上是合理的；后续若再做体验优化，建议把“当前原值来自最近一次测试”明确提示给用户。

## 移除内置站点语义与配置页看板编辑器

目标：公开发布主代码不再携带任何真实站点信息，站点配置只来自 `chrome.storage.local` 中的用户导入、用户新增或 demo 示例。

### 改动清单

- 改造 [site-config-shared.js](../site-config-shared.js)：
  - demo 卡片标题改为“单击”“长按”。
  - 简化站点配置协议，生产链路不再处理默认站点、内置站点、隐藏站点或 `removed: true` 条目。
  - `mergeStoredSiteConfigs()` 只从 storage 配置构建运行态站点；`deleteSiteConfigEntry()` 统一直接过滤删除。
  - 401 / 403 鉴权错误信息明确提示 token 失效或缺少鉴权信息。
- 改造 [popup.js](../popup.js)：
  - demo 初始显示值改为“点我”“拖我”。
  - 移除旧站点诊断集合、删除站点集合和 hidden/removed 持久化兼容路径。
  - 修复配置变更后看板显示异常：先清理无效 groups，再用清理后的有效 group id 过滤 `manualOrder`，避免保留悬空 `group:` 顺序项。
- 改造 [popup.html](../popup.html)：
  - popup 宽度从 420px 调整为 480px。
- 改造 [add-site.js](../add-site.js)：
  - 删除初始化自动加载 `site-config.public.json` 的路径，模板 JSON 只保留为用户手动导入文件。
  - 删除 `includeDefaultSites`、`builtinIds`、内置站点删除提示和 removed 删除语义。
  - 新增配置页草稿看板控制器，支持读取当前卡片/编组、打开单卡片菜单、打开编组菜单、重命名组、草稿删除站点、创建编组、重排和保存到看板。
  - 保存草稿时一次性写入 `siteConfigs`、`boardData`、`groups`、`manualOrder`。
- 改造 [add-site.html](../add-site.html)：
  - 从“新增/修改双模式表单页”重构为“配置页看板编辑器”结构。
  - 默认提供当前卡片看板、从文件导入、导出配置、新增站点、保存到看板、单卡片菜单、编组菜单、单站点配置面板和组名编辑面板。

### 新增/更新测试

- 更新 [task-demo-cards.test.js](../task-demo-cards.test.js)，锁定 demo 短文案和短显示值。
- 更新 [task-popup-ui.test.js](../task-popup-ui.test.js)，锁定 popup 宽度 480px。
- 更新 [task-add-site-ui.test.js](../task-add-site-ui.test.js)，锁定配置页看板编辑器结构。
- 更新 [task-add-site-flow.test.js](../task-add-site-flow.test.js)，覆盖配置页草稿菜单、删除、保存同步和不自动 fetch 模板 JSON。
- 更新 [task-site-config-state.test.js](../task-site-config-state.test.js)，覆盖 popup 吸收配置变更、清理悬空 groups/manualOrder/boardData。
- 更新 [task-import-export.test.js](../task-import-export.test.js)，覆盖配置页草稿编组、重命名、重排和保存。
- 新增 [task-no-builtin-sites.test.js](../task-no-builtin-sites.test.js)，扫描生产代码不含旧真实站点关键词和内置/隐藏站点语义关键词。
- 调整 VM sandbox 测试里的 `deepStrictEqual` 断言，对跨 context 对象先做 JSON 序列化，避免原型差异导致视觉一致但断言失败。

### 回归结果

已通过：

- `for f in /e/vscode/009_chromeExt/task-*.test.js; do node "$f" || exit 1; done`
- `node --check "/e/vscode/009_chromeExt/popup.js"`
- `node --check "/e/vscode/009_chromeExt/add-site.js"`
- `node --check "/e/vscode/009_chromeExt/site-config-shared.js"`

### 简短复盘

- 值得保留：先用测试扫描生产代码禁用旧站点关键词，再清理共享协议和页面逻辑，可以防止“看似空白态，实际主代码仍残留站点语义”。
- 值得保留：将 popup 显示异常归结到状态清理顺序，先清理 group 再清理 `manualOrder`，比在渲染层兜底更稳定。
- 可以优化：配置页已完成结构和控制器测试，但浏览器里的长按拖拽、菜单定位和保存后 popup 可视同步仍需要人工验收后再视为 UI 完成。

## 配置页点击路线重构与公式运算

目标：按用户梳理的点击路线重构配置页，首页只显示当前卡片展示区和 `新增站点` / `导入配置` / `导出配置` 3 个按钮，所有表单只在用户进入对应步骤后出现。

### 改动清单

- 改造 [add-site.html](../add-site.html)：
  - 新增独立首页 `homePanel`，首页只保留卡片展示区、导入/导出提示和 3 个主按钮。
  - 隐藏单站点配置区 `siteConfigPanel` 与编组配置区 `groupEditPanel`，避免初始页面堆满配置项。
  - 新增站点字段顺序固定为：站点名 → 接口地址 → 监控字段。
  - 初始错误提示改为空且隐藏，只在实际测试、导入或校验失败后显示。
  - 运算步骤从旧 `divideByInput` 改为公式输入，展示原值 `A`、快捷公式 `A/500000` / `180-A`、预览值和“完成”按钮。
  - 删除旧卡片/编组弹出菜单和“不需要”按钮。
- 改造 [site-config-shared.js](../site-config-shared.js)：
  - 新增 `normalizeFormula()` 和 `applyOptionalFormula()`。
  - 空公式直接返回原值。
  - 公式只允许 `A`、数字、括号和基础四则运算，非法公式返回 `kind: 'validation'`。
  - 旧 `divideBy` 仅作为兼容输入转换为 `A/<divideBy>`，保存和导出统一写 `formula`。
- 改造 [add-site.js](../add-site.js)：
  - `createPageApp()` 增加 `home` / `siteForm` / `calculation` / `groupEdit` 视图切换。
  - 页面初始化调用看板草稿加载并渲染 `configBoard`，修复配置页看不到当前卡片的问题。
  - 新增和编辑完成后刷新首页卡片展示区，不再调用 `window.close()`。
  - 单卡片点击直接进入该站点配置页，不触发接口刷新，也不再弹旧菜单。
  - 导入支持旧数组格式和完整看板包 `{ siteConfigs, groups, manualOrder, boardData }`，导入后立即刷新卡片展示区。
  - 导出完整看板包，并过滤 demo 站点、demo 排序和 demo boardData。
- 更新 [popup.js](../popup.js) 相关状态测试，确认 storage 变更后 popup 运行态能吸收 `formula` 配置并恢复 `extract()`。

### 新增/更新测试

- 更新 [task-add-site-ui.test.js](../task-add-site-ui.test.js)，锁定首页结构、字段顺序、公式 UI、无初始“错误提示”、无旧菜单和无旧除数输入。
- 更新 [task-add-site-flow.test.js](../task-add-site-flow.test.js)，覆盖配置页渲染当前卡片、单卡点击进入配置页、新增/编辑完成后回首页且不关闭页面。
- 新增 [task-formula-calculation.test.js](../task-formula-calculation.test.js)，覆盖空公式、`A/500000`、`180-A`、非法公式、旧 `divideBy` 兼容和运行态 `extract()`。
- 更新 [task-import-export.test.js](../task-import-export.test.js)，覆盖完整看板包导入导出、导入后刷新卡片区、非法 JSON 留在首页提示、导出过滤 demo。
- 更新 [task-site-config-state.test.js](../task-site-config-state.test.js)，将持久化和运行态断言从 `divideBy` 更新为 `formula`。

### 回归结果

已通过：

- `for f in /e/vscode/009_chromeExt/task-*.test.js; do node "$f" || exit 1; done`
- `node --check "/e/vscode/009_chromeExt/popup.js"`
- `node --check "/e/vscode/009_chromeExt/add-site.js"`
- `node --check "/e/vscode/009_chromeExt/site-config-shared.js"`

### 人工验收状态

- 自动化测试和语法检查已通过。
- 当前环境未完成浏览器/扩展中的可视化人工验收；UI 交互仍需要在 Chrome 扩展环境中逐项确认：首页、导入/导出、新增公式、单卡编辑、编组编辑、拖拽排序/编组和 popup 同步。

### 简短复盘

- 值得保留：先把用户点击路线整理成测试边界，再按红绿节奏改共享协议、HTML 和页面控制器，能避免继续沿着旧“管理表单页”设计扩散。
- 值得保留：完整看板包导入导出统一走 `siteConfigs` / `groups` / `manualOrder` / `boardData`，比只导出站点数组更符合配置页看板编辑模型。

## 配置页新增按钮与 demo 卡片补漏

- 修复配置页首页“新增站点”按钮点击无反应：补齐 `addSiteConfigBtn` DOM 引用和点击绑定，点击后进入新增站点配置页，并保持初始错误提示隐藏。
- 修复配置页直接打开时看不到两张示例卡片：配置页看板草稿在 storage 无站点且尚未标记 `_demoInjected` 时，会以只读草稿方式展示 `单击 / 长按` 两张 demo 卡片及 `点我 / 拖我` 初始值。
- 修复 `configBoard.replaceChildren()` 清空空状态节点后未重新挂载的问题，避免空态提示节点脱离 DOM。
- 更新 [task-add-site-flow.test.js](../task-add-site-flow.test.js)，新增配置页空 storage 显示 demo 卡片、点击新增进入新增表单、首页/表单显隐和初始错误提示隐藏断言。

### 回归结果

已通过：

- `node "/e/vscode/009_chromeExt/task-add-site-flow.test.js"`
- `node "/e/vscode/009_chromeExt/task-demo-cards.test.js"`
- `node "/e/vscode/009_chromeExt/task-import-export.test.js"`
- `for f in /e/vscode/009_chromeExt/task-*.test.js; do node "$f" || exit 1; done`
- `node --check "/e/vscode/009_chromeExt/add-site.js"`
- `node --check "/e/vscode/009_chromeExt/popup.js"`
- `node --check "/e/vscode/009_chromeExt/site-config-shared.js"`

### 简短复盘

- 值得保留：用户反馈 UI 无响应后，先补点击路线和 demo 空态测试，再改绑定与渲染逻辑，能直接锁住真实回归点。
- 可以优化：配置页 demo 目前是草稿展示，仍需在 Chrome 扩展环境人工确认首页视觉、按钮点击和 popup demo 注入时序是否完全符合预期。

## 配置页编组完成、删除与取消返回修复

- 修复进入编组配置后点击“完成”无响应的问题：补齐 `finishGroupEditBtn` 绑定，记录当前编组 id，并在完成时保存组名后返回配置页首页。
- 修复编组名称可能被异步加载旧值覆盖的问题：点击编组时先用当前卡片数据同步填充组名，再异步补充组内卡片名称输入，避免用户刚输入的新组名被回写成旧组名。
- 修复单卡编辑页删除链路：删除时复用看板草稿控制器清理 `siteConfigs`、`boardData`、`groups` 和 `manualOrder`，保存后返回首页。
- 新增单卡编辑页“取消”按钮：点击后不写入 storage，清空当前选择并返回配置页首页。
- 更新 [task-add-site-flow.test.js](../task-add-site-flow.test.js)，覆盖编组完成返回、单卡删除清理和取消修改不落盘。

### 回归结果

已通过：

- `node "/e/vscode/009_chromeExt/task-add-site-flow.test.js"`
- `node "/e/vscode/009_chromeExt/task-import-export.test.js"`
- `node "/e/vscode/009_chromeExt/task-demo-cards.test.js"`
- `for f in /e/vscode/009_chromeExt/task-*.test.js; do node "$f" || exit 1; done`
- `node --check "/e/vscode/009_chromeExt/add-site.js"`
- `node --check "/e/vscode/009_chromeExt/popup.js"`
- `node --check "/e/vscode/009_chromeExt/site-config-shared.js"`

### 人工验收状态

- 自动化测试和语法检查已通过。
- 仍需在 Chrome 扩展环境人工确认：编组完成回首页、单卡删除回首页、取消修改回首页，以及 popup 看板是否同步反映删除和组名/卡片名变更。

### 简短复盘

- 值得保留：对用户反馈的三个交互断点分别补测试，再按失败点逐个修复，能避免只修按钮绑定却漏掉 storage 清理或取消不落盘。
- 可以优化：配置页的真实浏览器长按、编组内点击和 popup 同步仍属于可视交互链路，后续需要配合人工验收结果继续收敛。

## 配置页新增流程返回与 demo 卡片编辑路线修复

- 修复新增流程缺少返回入口的问题：在 [add-site.html](../add-site.html) 的新增流程操作区补上独立 `createBackHomeBtn`，不再复用编辑态 `backHomeBtn`，避免新增/编辑语义混淆。
- 修复 [add-site.js](../add-site.js) 中新增流程无法稳定取消的问题：在 `createDomRefs()` 和 `bind()` 中接入 `createBackHomeBtn`，点击后统一复用 `returnHomeWithoutSaving()` 回到配置页首页，且不写入 `chrome.storage.local`。
- 修复 demo 卡片点击语义：配置页首页示例卡片点击后不再走“新增站点”或“从 storage 选真实站点”的链路，而是通过 `openSiteEdit()` / `buildDemoEditSite()` 直接进入 `siteForm:edit` 编辑路线。
- 同时修复单卡编辑完成链路的状态耦合：打开真实卡片编辑页时先同步设置 `selectedSiteId`，避免测试和实际操作中出现“请先选择需要修改的站点”。
- 更新 [task-add-site-flow.test.js](../task-add-site-flow.test.js)，补充两组断言：
  - 点击“新增站点”后，点击“取消”应返回首页且不落盘。
  - storage 为空时，点击 demo 卡片应进入编辑页，取消返回首页，且不写入 storage。

### 回归结果

已通过：

- `node "/e/vscode/009_chromeExt/task-add-site-flow.test.js"`
- `node "/e/vscode/009_chromeExt/task-demo-cards.test.js"`
- `node "/e/vscode/009_chromeExt/task-import-export.test.js"`
- `for f in /e/vscode/009_chromeExt/task-*.test.js; do node "$f" || exit 1; done`
- `node --check "/e/vscode/009_chromeExt/add-site.js"`
- `node --check "/e/vscode/009_chromeExt/popup.js"`
- `node --check "/e/vscode/009_chromeExt/site-config-shared.js"`

### 人工验收状态

- 自动化测试和语法检查已通过。
- 仍需在 Chrome 扩展环境人工确认：新增流程点击“取消”能返回首页且不落盘；demo 卡片点击后进入编辑路线、返回首页后不产生多余 storage 写入；真实站点卡片、编组卡片和 demo 卡片三条点击路线互不串线。

### 简短复盘

- 值得保留：这次先让新增流程取消和 demo 编辑路线测试真正打出红灯，再补最小 DOM 与路由改动，能更快锁定交互断点而不是在 UI 表面兜圈子。
- 可以优化：demo 目前仍是“只读草稿入口”语义，后续如果用户继续收紧产品定义，可能还需要在页面提示上进一步区分“示例编辑”与“真实站点编辑”。
