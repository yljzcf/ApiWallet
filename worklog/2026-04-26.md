# 2026-04-26

## 009_chromeExt 第一阶段改进
- 为插件补充图标资源目录 `icons/`，新增 `icon-16.png`、`icon-32.png`、`icon-48.png`、`icon-128.png`。
- 更新 `manifest.json`，补充 `icons` 与 `action.default_icon`，版本提升到 `1.1`。
- 重写 popup 视觉样式与结构，改为更清晰的头部信息区、状态区和 2 列卡片布局，提升可读性。
- 调整刷新逻辑：
  - 单项刷新失败时保留原值，并显示错误状态。
  - 批量刷新改为统一抓取后再集中更新缓存，减少重复持久化。
  - 区分“上次全量同步”和“最近一次批量刷新”语义。
  - 增加批量刷新与单卡刷新互斥、按钮/卡片禁用与 finally 收口，避免竞态和卡死。

## 验证记录
- 已验证 `popup.js` 语法通过：`node --check /e/vscode/009_chromeExt/popup.js`
- 已验证 `manifest.json` 为合法 JSON。
- 已验证图标文件尺寸与 manifest 引用路径存在。
- 未在当前环境中实际打开浏览器扩展界面做视觉验收；仍需用户在浏览器中加载扩展后确认展示效果。

## Task 1：异常站点最小可诊断能力
- 在 `/e/vscode/009_chromeExt/popup.js` 为异常站点新增最小诊断辅助：
  - 对 `ikun-api`、`yybb-api`、`yybb-api2`、`发现-api` 的 401/403 响应，明确提示“认证可能失效，请检查 Bearer token 是否已过期”。
  - 对 200 但关键字段缺失的场景，改为抛出“接口字段缺失（接口字段可能变更）”，不再回落成笼统的“未找到quota”。
  - 对 `yybb-api` 的 `subscriptions` 空数组场景，单独提示“未找到订阅数据（subscriptions 为空数组）”。
- 保持 `fetchTaskValue` / `fetchSingleData` / `fetchAllData` 既有整体结构，只在 `fetchTaskValue` 前增加 HTTP 错误归一化，在目标站点提取函数内改为显式抛错。
- 继续保留 `yybb-api` 现有 `200 - weekly_usage_usd` 公式，但在字段不存在时明确失败，不伪造成功值。

## 验证记录（Task 1）
- 已再次验证 `popup.js` 语法通过：`node --check /e/vscode/009_chromeExt/popup.js`
- 已用静态检索确认新增错误分支已接入：认证失效、字段缺失、`subscriptions` 空数组三类提示都存在于 `popup.js`。
- 由于当前环境无法访问目标站点真实接口，尚未完成真实 token / 真实响应下的联调；若线上 token 已过期，界面会明确提示需要检查 Bearer token。

## 简短复盘
- 值得保留：先把“HTTP 状态错误”和“200 但结构异常”拆开，再在目标站点上做最小定点修复，能在不改主流程的前提下提升可诊断性。
- 可优化：后续若这类 Bearer token 站点继续增多，可把“认证依赖站点列表”和“字段提取校验”抽成更清晰的配置层能力，减少散落的定制分支。

## Task 2：统一 popup 持久化状态入口
- 在 `/e/vscode/009_chromeExt/popup.js` 新增统一状态键集合与最小规范化逻辑：
  - 持久化状态结构覆盖 `boardData`、`lastUpdateTime`、`autoSortEnabled`、`layoutColumns`、`manualOrder`、`groups`。
  - `layoutColumns` 仅允许 `2 | 3 | 4`，非法值回退为 `2`。
  - `groups` 仅保留形如 `{ id: 'group:x', taskIds: [...] }` 且 `taskIds` 全为字符串的项；非法项过滤。
- 新增 `loadPopupState()`：统一从 `chrome.storage.local` 读取所需 key，提供默认值并做最小规范化。
- 新增 `persistPopupState(partialState)`：统一写入 `autoSortEnabled`、`layoutColumns`、`manualOrder`、`groups`，并按需附带写入 `boardData` / `lastUpdateTime`。
- 将现有 `init()`、`persistBoardData()`、批量刷新里的 `lastUpdateTime`/`boardData` 持久化改为走统一入口，为后续 Task 3-6 铺底。
- 保持 `fetchTaskValue`、`fetchSingleData`、`fetchAllData` 的主体行为不变，未接入 UI 控件、`viewItems` 或拖拽逻辑。

## 验证记录（Task 2）
- 先编写一次性脚本 `/e/vscode/009_chromeExt/task2-partial-persist.test.js`，验证新入口在以下场景下的行为：
  - 读取时会回退非法 `layoutColumns`。
  - 读取时会过滤非法 `groups`。
  - `persistPopupState({ boardData })` 不会重置已存在的 `autoSortEnabled`、`layoutColumns`、`manualOrder`、`groups`。
  - 写入时会按需包含 `boardData` / `lastUpdateTime`。
- 已确认该一次性脚本先失败（partial 写入会把未传字段重置为默认值），修正 `persistPopupState()` 后转为通过；随后已删除该脚本，避免残留。
- 已验证 `popup.js` 语法通过：`node --check /e/vscode/009_chromeExt/popup.js`

## 简短复盘（Task 2）
- 值得保留：先把“读取规范化”和“统一写入口”补齐，再只替换初始化与持久化接缝，能在低扰动前提下给后续排序/分组能力打底。
- 可优化：当前 `persistBoardData()` / 全量刷新仍需先读一次现有状态再回写；后续若状态项继续增多，可考虑在内存中维护完整 popup state，减少重复读存储。

## Task 3：统一视图项模型与静态分组渲染
- 在 `/e/vscode/009_chromeExt/popup.js` 引入统一视图项模型：
  - 单卡使用 `{ id: 'task:xxx', type: 'single', taskId: 'xxx' }`。
  - 双卡组使用 `{ id: 'group:x', type: 'group', taskIds: ['a', 'b'] }`。
- 新增 `buildGroupIndex(groups)`、`buildViewItems(groups, manualOrder)`、`buildTaskToViewItemMap(viewItems)`，由 `CONFIG + groups + manualOrder` 生成当前应展示的视图项列表与任务到视图项索引。
- 将原先基于 `CONFIG.forEach(...)` 的静态卡片渲染改为：
  - `renderBoard(viewItems)`
  - `renderSingleCard(item)`
  - `renderGroupCard(item)`
- 将 refs 管理从 `taskId -> refs` 升级为 `viewItemId -> refs`，并维护 `taskId -> viewItemId` 索引，使单卡与组卡都能走统一刷新入口。
- 适配 `fetchSingleData()` 与 `fetchAllData()`：
  - 保持 `fetchTaskValue` 主体逻辑不变。
  - 单任务刷新时，若任务属于组卡，只更新该组卡内部对应数值行，并沿用现有加载/成功/失败文案风格。
  - 组卡点击会并发触发其两个任务的单项刷新；本步未接拖拽、自动排序、折叠菜单和列数切换。
- 在 `/e/vscode/009_chromeExt/popup.html` 补充组卡最小样式，确保组卡能展示两个站点名和两行数值区域。

## 验证记录（Task 3）
- 已验证 `popup.js` 语法通过：`node --check /e/vscode/009_chromeExt/popup.js`
- 先编写一次性脚本 `/e/vscode/009_chromeExt/task3-group-regression.test.js`，验证两类关键回归：
  - 重叠 task 不会同时进入多个 group；重复占用 task 的后续 group 会被跳过。
  - 组内仍有任务处于 `loading` 时，整张组卡会保持 `loading`，不会被已完成任务提前覆盖成 `success` / `error`。
- 已确认该一次性脚本先失败（允许重叠 group、且缺少组级状态汇总），修正 `buildGroupIndex()` 与组卡状态汇总逻辑后转为通过；随后已删除该脚本，避免残留。
- 已静态检查确认：
  - 无分组时仍可按单卡列表渲染。
  - 有 `groups` 持久化状态时会优先渲染双卡组，其余任务继续按单卡渲染。
  - 刷新逻辑已改为通过 `taskId -> viewItemId` 映射定位组卡内对应数值区域，并按组级状态汇总更新整卡状态 pill / meta。
- 当前环境未实际打开浏览器 popup 做界面验收，因此组卡的最终视觉与交互仍需用户在浏览器中确认。

## 简短复盘（Task 3）
- 值得保留：先抽象 view item 与索引层，再把渲染与刷新接缝整体迁移，能在不提前接入拖拽逻辑的情况下先完成静态分组落地。
- 可优化：当前组卡状态 pill 与 meta 仍是整卡共享，若后续需要更细粒度地表达组内两个任务各自状态，可能还要继续细化到“组级 + 行级”双层状态模型。

## Task 4：轻量折叠菜单与多列布局
- 在 `/e/vscode/009_chromeExt/popup.html` 精简 popup 文案：
  - 移除头部副文案、状态区解释文案、底部提示文案。
  - 保留最小必要标题与“全部同步”入口。
- 在头部加入轻量折叠菜单：
  - 新增菜单按钮，可展开/收起设置区。
  - 设置区包含“自动排序”开关与“单行卡片数”下拉（2 / 3 / 4）。
- 将 `.board` 的列数改为由 `layoutColumns` 驱动，并通过 `data-columns` + CSS 变量控制：
  - 2 列维持较大字号与 spacing。
  - 3 列缩小 card padding、title、value 与组卡间距。
  - 4 列进一步压缩，但保留组卡两行文本的可读性。
- 在 `/e/vscode/009_chromeExt/popup.js` 接入统一状态流：
  - 新增 `applyLayoutColumns()`、`applyAutoSortEnabled()`、`setMenuOpen()`、`rebuildBoard()`。
  - 初始化时从持久化状态恢复 `layoutColumns` / `autoSortEnabled`，并同步到控件与布局。
  - 切换自动排序时仅更新统一状态、持久化并重新渲染当前 `viewItems`，不实现真实排序算法，为 Task 6 铺底。
  - 切换列数时立即更新布局并持久化，确保重新打开 popup 后设置仍保留。
- 同步收敛状态文案密度：
  - 全局同步区改为更短的状态文本。
  - 默认卡片 meta 清空，仅在加载/成功/失败时显示必要状态。

## 验证记录（Task 4）
- 已验证 `popup.js` 语法通过：`node --check /e/vscode/009_chromeExt/popup.js`
- 已静态检查确认：
  - 菜单展开/收起由 `menuToggleBtn` + `setMenuOpen()` 控制。
  - 列数切换会同步更新 `board` 的 `--board-columns` 与 `data-columns`。
  - `autoSortEnabled` 与 `layoutColumns` 都会写回 `chrome.storage.local`，下次打开 popup 可恢复。
- 当前环境未实际打开浏览器 popup，因此菜单动画、2/3/4 列视觉密度与最终交互体验仍需用户在扩展 popup 中实际验收。

## 简短复盘（Task 4）
- 值得保留：先把列数与开关都接到统一状态入口，再让 UI 只消费状态，后续补排序算法时改动面会更小。
- 可优化：当前自动排序开关只触发重渲染、尚未影响 `buildViewItems()` 的排序结果；Task 6 可以直接在现有状态流上补正式排序逻辑。

## Task 5：长按拖拽、双卡编组与长按拆组
- 在 `/e/vscode/009_chromeExt/popup.js` 为卡片交互补齐最小拖拽状态：
  - 新增 `longPressTimer`、`dragState`、拖拽预览节点与 `cancelLongPress()` / `startLongPressDrag()` / `updateDragPreview()` / `finishDrag()`。
  - 单卡长按进入拖拽态；短点击保持原刷新语义，不会误触拖拽。
  - 组内成员长按时会先 `ungroupByTaskId(taskId)`，保留另一张卡在原位，被拖起的成员继续跟随指针。
- 补充手动编排能力：
  - `autoSortEnabled=false` 时支持自由换位，并把结果写回 `manualOrder`。
  - `autoSortEnabled=true` 时不做自由换位，但仍允许独立单卡拖到另一张独立单卡上编组。
  - 通过 `canGroup(taskA, taskB)` 限制只允许两张独立单卡组成双卡组，不允许第三张卡进入已有组。
- 调整卡片 DOM 语义与最小样式：
  - 外层 `.card` 改为拖拽容器，内部 `.card-main` 保留点击区。
  - 单卡头部新增明确的“刷新”按钮；组卡保留整卡刷新，并为每个组内成员补单独刷新按钮，避免与长按拖拽冲突。
  - 新增拖拽态、高亮投放态、拖拽预览样式，支撑换位/编组反馈。
- 保持本步边界：未实现 Task 6 的真实排序算法，也未新增 commit / worktree / 持久化之外的额外机制。

## 验证记录（Task 5）
- 已先做一次性失败验证，确认 `popup.js` 初始不存在 Task 5 所需的拖拽/编组函数入口；实现后再次静态校验通过。
- 已验证 `popup.js` 语法通过：`node --check /e/vscode/009_chromeExt/popup.js`
- 已静态确认以下规则已接入代码：
  - 自动排序关闭时允许自由换位并持久化 `manualOrder`。
  - 自动排序开启时禁止自由换位，但仍允许独立单卡之间编组。
  - 仅允许两张独立单卡编组，组内成员长按会先拆组再拖动。
- 当前环境未实际打开浏览器 popup 做交互验收，因此长按阈值手感、拖拽命中阈值与最终视觉反馈仍需用户在扩展中实际确认。

## 简短复盘（Task 5）
- 值得保留：继续复用 `viewItems / groupsCache / manualOrderCache / rebuildBoard()`，能把换位、编组、拆组都收敛到同一视图模型上，避免额外分叉状态。
- 可优化：当前投放命中与重叠阈值使用的是前端静态几何近似；后续若需要更稳定的拖拽体验，可在不改数据结构的前提下继续微调阈值与命中策略。

## Task 5 补充修正：拆组持久化与原位顺序
- 在 `/e/vscode/009_chromeExt/popup.js` 针对主线程复审发现的 Task 5 缺口补最小修正：
  - 组内成员长按拆组后，即使最后没有命中新的编组或换位目标，仍会把拆组结果持久化。
  - `ungroupByTaskId(taskId)` 改为以“拆组前的 group 所在位置”为锚点，把“留在原位的卡 + 被拖起的卡”插回原组位置，避免掉回全局默认顺序。
- 未扩展 Task 5 范围外能力；只修复“拆组后 UI 与持久化状态不一致”这一实际规格缺口。

## 验证记录（Task 5 补充修正）
- 先编写一次性脚本 `/e/vscode/009_chromeExt/task5-ungroup-persist.test.js`，验证：
  - 组内成员长按拆组后，即使松手在空白处，也会持久化拆组结果。
  - 拆组后 `manualOrder` 会把“留在原位的卡”和“被拖起的卡”插回原组位置，而不是落回默认全量顺序。
- 已确认该一次性脚本先失败（未持久化拆组结果，且顺序锚点错误），修正 `finishDrag()` 与 `ungroupByTaskId()` 后转为通过；随后已删除该脚本，避免残留。
- 已再次验证 `popup.js` 语法通过：`node --check /e/vscode/009_chromeExt/popup.js`

## Task 6：自动排序与刷新后的重排规则
- 在 `/e/vscode/009_chromeExt/popup.js` 新增最小排序逻辑：
  - `parseComparableValue(taskId, rawValue)`：把可比较余额解析为数字；错误文案、空值和不可解析值返回 `null`。
  - `getViewItemSortValue(item)`：单卡取自身余额；组卡取两张余额和；任一成员不可解析则整组视为不可排序。
  - `sortViewItemsIfNeeded(items)`：在 `autoSortEnabled=true` 时按余额降序排序，并用原始索引兜底，避免失败值之间抖动。
  - `reorderBoardDomIfNeeded()`：在不重建整棵卡片 DOM 的前提下，按排序结果重排现有卡片节点。
- 将排序接入这些场景：
  - 初始化 `rebuildBoard()` 时。
  - 单卡刷新成功后。
  - 批量刷新完成后。
  - 自动排序开关切换后（通过 `rebuildBoard()`）。
  - 编组 / 拆组后（通过 `rebuildBoard()`）。
- 保持已批准交互边界：
  - `autoSortEnabled=true` 时继续禁止自由换位。
  - 但独立单卡之间仍可拖拽编组。

## 验证记录（Task 6）
- 先编写一次性脚本 `/e/vscode/009_chromeExt/task6-auto-sort.test.js`，验证：
  - 单卡余额可解析时参与排序，不可解析值排到末尾。
  - 组卡按两张卡余额和排序。
  - 自动排序关闭时保持当前顺序不变。
  - 单卡刷新成功后，即使值未变化，也会尝试执行一次重排收口。
- 已确认该一次性脚本先失败（缺少排序函数，后续又暴露“值未变化时不会尝试重排”的触发缺口），补齐排序函数和重排触发点后转为通过；随后已删除该脚本，避免残留。
- 已再次验证 `popup.js` 语法通过：`node --check /e/vscode/009_chromeExt/popup.js`

## 简短复盘（Task 6）
- 值得保留：把“排序值计算”和“DOM 重排收口”分开，能在不改 `groups/manualOrder/viewItems` 数据结构的前提下补上自动排序。
- 可优化：当前 `parseComparableValue()` 仍按通用数字字符串解析；若后续站点返回格式出现货币符号、单位或前后缀文案，可再按 `taskId` 补更细的定制解析规则。

## Task 7：popup 极简瘦身（按用户新反馈）
- 在 `/e/vscode/009_chromeExt/popup.html` 将 popup 外壳整体压缩为更紧凑的暗色仪表盘：
  - 移除顶部标题区、同步状态区和多余留白，只保留右上角的“同步”按钮与折叠菜单入口。
  - 重写卡片样式，改为更低高度、更小圆角、更紧凑内边距的极简卡片。
  - 强化 4 列模式的尺寸收缩，进一步下调最小高度、字号、间距与圆角。
- 在 `/e/vscode/009_chromeExt/popup.js` 收紧展示信息与交互语义：
  - 单卡只显示 `name` 和额度值。
  - 组卡每行只显示 `name` 和额度值。
  - 移除状态 pill、更新时间 / meta 文案、单卡刷新按钮、组内刷新按钮。
  - 恢复“点击卡片本体刷新”作为唯一显式刷新入口；拖拽仍走长按逻辑。
  - 保留内部 `data-state`，仅用于最小视觉边框反馈，不再额外显示文字状态。

## 验证记录（Task 7：popup 极简瘦身）
- 已验证 `popup.js` 语法通过：`node --check /e/vscode/009_chromeExt/popup.js`
- 已验证 `manifest.json` 为合法 JSON：`python -m json.tool /e/vscode/009_chromeExt/manifest.json >/dev/null`
- 当前环境仍未实际打开浏览器 popup，因此这轮仅完成静态瘦身与结构收口；最终“是否足够简洁、4 列是否达到你要的密度”仍需你在扩展 popup 里直接验收。

## 简短复盘（Task 7：popup 极简瘦身）
- 值得保留：先把“信息层级”直接砍到只剩名字和值，再做视觉压缩，比单纯调 CSS 更能快速接近你给的示例方向。
- 可优化：如果你下一轮还觉得 4 列模式偏大，最直接的继续收口点会是 `body.width`、4 列 `--value-size` 和组卡两行的行高，而不是再加新的控件或说明文字。

## 新一轮：yybb 下线、诊断、配置层、右键操作与紧凑排布
- 在 `/e/vscode/009_chromeExt/popup.js` 移除了 `yybb-api` / `yybb-api2` 默认站点，并在 `loadPopupState()` 的状态清洗阶段同步清理：
  - `boardData` 中对应缓存值
  - `manualOrder` 中对应 `task:*` 引用
  - `groups` 中包含失效 taskId 的组
- 查清 `ikun-api` 与 `发现-api` 的真实失败原因：当前线上接口均直接返回 `401`，应用层 payload 为 `TOKEN_EXPIRED`；界面侧改为明确显示 `Token过期` / `认证失效(401)` / `接口异常(code)` 这类可行动结果，并在控制台输出定点诊断（HTTP 状态、顶层 key、`data` key、`balance` 是否存在、应用层 code/message）。
- 将站点来源从“仅运行时代码常量”升级为“默认种子配置 + `chrome.storage.local.siteConfigs` 持久化配置”模型：
  - 运行时仍保留 `extract` 等函数能力。
  - 落盘时只序列化可存储字段，不写入函数。
  - 删除卡片与 token 更新都基于这层配置状态进行持久化。
- 为 popup 增加右键毛玻璃操作层：
  - 右键卡片会打开轻量浮层，展示齿轮 / 垃圾桶两个操作。
  - 组卡右键按鼠标所在行的 task 生效，而不是整组生效。
  - 齿轮会打开轻量弹层编辑该 task 当前请求头中的主 token 字段；保存后写回 `siteConfigs`。
  - 垃圾桶会删除当前站点，并同步清理 `siteConfigs / boardData / manualOrder / groups`。
- 为删除场景补了并发回流防护：若站点在刷新过程中被删除，返回结果不会再把已删除 task 的数据写回缓存或界面。
- 将排序与布局升级为“显式排布 + 无空洞优先”：
  - 新增 `sortViewItemsForLayout()`，在自动排序与手动模式下都允许组卡在必要时前置一位，避免单卡在前造成明显空洞。
  - 新增 `buildLayoutSlots()` 与 `applyLayoutToCard()`，单卡视为 1 个高度单元、组卡视为 2 个高度单元，由 JS 生成显式 `grid-row/grid-column` 占位。
  - `.board` 改为消费显式占位结果；组卡不再把同排其他卡片强行拉高，旁边单卡会补入剩余逻辑空位。

## 验证记录（新一轮）
- 已通过一次性脚本验证 yybb 清理与余额提取分类：`node /e/vscode/009_chromeExt/task-yybb-diagnostics.test.js`
- 已通过一次性脚本验证配置序列化、删卡状态清理与组内删除回位：`node /e/vscode/009_chromeExt/task-site-config-state.test.js`
- 已通过一次性脚本验证“组卡前置避坑”与显式布局占位：`node /e/vscode/009_chromeExt/task-compact-layout.test.js`
- 已再次验证 `popup.js` 语法通过：`node --check /e/vscode/009_chromeExt/popup.js`
- 已再次验证 `manifest.json` 为合法 JSON：`python -m json.tool /e/vscode/009_chromeExt/manifest.json >/dev/null`
- 已独立核实 `ikun-api` / `发现-api` 当前真实响应为 `401 Unauthorized`，body 为 `{"code":"TOKEN_EXPIRED","message":"Token has expired"}`。
- 已补充回归验证“切换 2 / 3 / 4 列后会立即重算显式布局槽位”，避免列数变化后继续沿用旧的 `grid-row/grid-column` 占位。
- 当前环境仍未实际打开浏览器扩展 popup，因此以下项仍需你在浏览器中人工验收：
  - 右键毛玻璃层的展示位置、关闭行为和按钮命中手感
  - token 弹层的录入与保存体验
  - 2 / 3 / 4 列下组卡与单卡的真实视觉密度
  - 自动排序 / 手动模式下“组合卡前置避坑”的最终观感是否完全符合预期

## 简短复盘（新一轮）
- 值得保留：先用一次性脚本锁住“状态清洗 / 配置序列化 / 布局占位”这三类容易回归的行为，再落代码，能明显减少 UI 任务里那种“看起来对了但状态已经坏了”的隐性问题。
- 可优化：当前 token 编辑器只覆盖站点请求头中的主 token 字段，还没有把“新增站点 / 编辑名称 / 编辑 URL / 多 header 站点完整配置”这类更完整的配置面板做出来；如果下一轮继续做配置体系，建议直接把右键弹层升级成完整站点编辑表单。
- 本轮补充修正：列数切换现在会立即 `rebuildBoard()`，确保 2 / 3 / 4 列切换时显式布局与避坑排序同步重算，而不是继续沿用旧列数的占位结果。

## 新一轮：popup 交互与尺寸收口
- 在 `/e/vscode/009_chromeExt/popup.js` 修正了左键轻点与长按拖拽之间的串扰：
  - 单卡与组卡都改为在 `pointerup` 轻点成功路径直接触发刷新，不再依赖浏览器后续合成 `click`。
  - 继续复用 `dragSuppressClick / dragInitiated / longPressTriggered` 现有标记，只收窄误判范围，避免长按拖拽后误刷或轻点双刷。
- 在 `/e/vscode/009_chromeExt/popup.html` 收口 popup 的紧凑设置区与尺寸语义：
  - 将“每行卡片”改为“卡片大小”，并保持内部存储值仍为 `2 | 3 | 4`，界面映射为 `大 / 中 / 小`。
  - 将“自动排序”和“卡片大小”压缩到同一行内，通过 `menu-row--compact` 与 `menu-field` 容器保持紧凑但不拥挤。
- 统一了卡片与组卡的文本与高度表现：
  - `.title` / `.group-name` 强制为单行省略，不再换行。
  - `.group-body` 改为填满整张双高卡片，使用两行等高网格，让组卡视觉高度更接近两张单卡上下堆叠。
- 收口右键菜单与名称编辑能力：
  - 右键菜单新增“修改站点名称”按钮，与 token / 删除并列。
  - 菜单 padding、gap、按钮尺寸、图标尺寸改为由 `data-columns` 下的 CSS token 驱动，小卡模式会同步缩小。
  - `openCardContextMenu()` 改为先显示菜单再按真实宽度定位，不再依赖写死偏移。
  - 新增 `siteNameEditor` 弹层，复用现有轻量编辑器模式修改站点显示名，并持久化到 `siteConfigs[].name`。
- 在 `/e/vscode/009_chromeExt/task-site-config-state.test.js` 补了显示名修改回归：
  - 新名称会去空白后写入。
  - 纯空白输入会被忽略，不覆盖已有显示名。
  - 删除其他站点后，已修改名称仍会保留。
- 新增并通过 `/e/vscode/009_chromeExt/task-popup-ui.test.js`，用静态断言锁住这轮 popup 结构与样式目标，避免后续把紧凑布局、单行文字或右键菜单尺寸改回去。

## 验证记录（popup 交互与尺寸收口）
- 已通过左键刷新回归：`node /e/vscode/009_chromeExt/task-click-refresh.test.js`
- 已通过 popup UI 结构回归：`node /e/vscode/009_chromeExt/task-popup-ui.test.js`
- 已通过站点配置状态回归：`node /e/vscode/009_chromeExt/task-site-config-state.test.js`
- 已通过显式布局回归：`node /e/vscode/009_chromeExt/task-compact-layout.test.js`
- 已再次验证 `popup.js` 语法通过：`node --check /e/vscode/009_chromeExt/popup.js`
- 已再次验证 `manifest.json` 为合法 JSON：`python -m json.tool /e/vscode/009_chromeExt/manifest.json >/dev/null`
- 当前环境仍未实际打开浏览器扩展 popup，因此以下项仍需你在浏览器中人工验收：
  - 单卡左键轻点是否稳定刷新
  - 组卡高度是否已经与两张单卡的视觉比例协调
  - 大 / 中 / 小三种尺寸下标题和值是否都保持单行
  - 右键菜单在 4 列小卡模式下是否已足够协调
  - “修改 / token / 删除”三个按钮的实际点击手感与名称修改持久化

## 简短复盘（popup 交互与尺寸收口）
- 值得保留：先用专项回归分别锁点击链路、popup 结构和配置状态，再去改 UI 与交互，可以把“看起来改好了但状态流坏掉”的风险压得比较低。
- 可优化：当前组卡高度的验收仍主要靠静态结构和浏览器肉眼确认；如果后续这块还反复波动，可以考虑再补一层更细的布局快照或 DOM 尺寸级测试。

## 新一轮：固定间距、分组名与组卡总高二次校准
- 在 `/e/vscode/009_chromeExt/popup.html` 重新校准了 popup 宽度与三档尺寸 token：
  - popup 宽度从 `404px` 微调到 `420px`，给组卡顶部标题和 3 / 4 列模式留出更均衡的横向空间。
  - 将 2 / 3 / 4 三种尺寸下的 `--board-gap` 统一固定为 `8px`，避免卡片变小时主间距跟着缩小。
  - 为组卡新增 `--group-gap` 与 `--group-header-height`，让“分组名 + 两行卡片”的总高能按统一公式对齐。
- 为组卡补了明确的顶部分组名区域：
  - 组卡新增 `.group-header` / `.group-title`，显示分组名；没有自定义分组名时回退为两张卡名的组合标题。
  - `.group-card` 显式使用 `min-height: calc(card * 2 + board-gap)`，让组卡总高对齐“两张未编组卡片 + 中间一条主间距”。
  - `.group-body` 改为 `auto + 2 行内容` 的网格，顶部标题和两张成员卡共用统一高度预算。
- 在 `/e/vscode/009_chromeExt/popup.js` 为分组右键命名补齐状态流：
  - `groups` 持久化结构现在支持 `name` 字段，并会在读取、规范化、渲染链路中保留。
  - 右键组卡顶部时，菜单“修改”会进入分组命名；右键组内成员行时，仍然是修改该站点名。
  - 新增 `renameGroup()`，保存后通过 `persistPopupState({ groups })` 落盘并即时重渲染。
- 在 `/e/vscode/009_chromeExt/task-site-config-state.test.js` 补了分组命名回归：
  - 分组名会去空白后写入。
  - 纯空白输入不会覆盖已有分组名。
- 在 `/e/vscode/009_chromeExt/task-popup-ui.test.js` 补了这轮结构断言：
  - 三档尺寸主间距固定为 `8px`。
  - 组卡顶部必须存在分组名区域。
  - 组卡总高公式必须显式对齐“两张单卡 + 一条卡片间距”。

## 验证记录（固定间距、分组名与组卡总高二次校准）
- 已通过 popup UI 结构回归：`node /e/vscode/009_chromeExt/task-popup-ui.test.js`
- 已通过站点 / 分组配置状态回归：`node /e/vscode/009_chromeExt/task-site-config-state.test.js`
- 已通过左键刷新回归：`node /e/vscode/009_chromeExt/task-click-refresh.test.js`
- 已通过显式布局回归：`node /e/vscode/009_chromeExt/task-compact-layout.test.js`
- 已再次验证 `popup.js` 语法通过：`node --check /e/vscode/009_chromeExt/popup.js`
- 已再次验证 `manifest.json` 为合法 JSON：`python -m json.tool /e/vscode/009_chromeExt/manifest.json >/dev/null`
- 当前环境仍未实际打开浏览器扩展 popup，因此这轮仍需你重点人工验收：
  - 2 / 3 / 4 三档下卡片之间的主间距是否看起来完全一致
  - 组卡顶部“分组名”区域是否顺眼，且右键修改手感是否符合预期
  - 组卡总高是否已经严格贴合“两张单卡 + 一条主间距”的视觉节奏

## 简短复盘（固定间距、分组名与组卡总高二次校准）
- 值得保留：先把“视觉一致性”拆成可断言的 token 和高度公式，再调样式，能避免一边改尺寸一边把结构约束改丢。
- 可优化：当前静态测试已经锁住了主间距和组卡高度公式，但最终是否“精致”仍取决于浏览器真实渲染；下一步最关键的仍是你肉眼验收。 

## 新一轮：中小卡组内行距、组头总余额与 token 更新
- 在 `/e/vscode/009_chromeExt/popup.html` 继续收紧中号 / 小号组卡的内部纵向节奏：
  - 中号卡把 `--group-gap` 从 `6px` 收到 `4px`。
  - 小号卡把 `--group-gap` 从 `6px` 收到 `3px`，同时继续下调组头高度，减少你指出的“中小卡行间距偏大”问题。
- 为组卡顶部增加总余额展示尝试：
  - 组头新增 `.group-total`，显示当前两张成员卡可解析余额之和。
  - 若组内有任一成员当前值不可解析，则组总余额回退为 `--`，避免误导性合计。
  - 这一块仅做轻量展示试验，便于你在真实 popup 里判断保留还是取消。
- 在 `/e/vscode/009_chromeExt/popup.js` 更新默认 token：
  - `ikun-api` 的 Bearer token 已替换为你提供的新值。
  - `发现-api` 的 Bearer token 已替换为你提供的新值。
- 在 `/e/vscode/009_chromeExt/task-popup-ui.test.js` 补了这轮结构断言：
  - 中号卡必须保留 `8px` 主间距，同时将组内间距压到 `4px`。
  - 小号卡必须保留 `8px` 主间距，同时将组内间距压到 `3px`。
  - 组头必须存在 `.group-total` 节点。

## 验证记录（中小卡组内行距、组头总余额与 token 更新）
- 已通过 popup UI 结构回归：`node /e/vscode/009_chromeExt/task-popup-ui.test.js`
- 已通过站点 / 分组配置状态回归：`node /e/vscode/009_chromeExt/task-site-config-state.test.js`
- 已通过左键刷新回归：`node /e/vscode/009_chromeExt/task-click-refresh.test.js`
- 已通过显式布局回归：`node /e/vscode/009_chromeExt/task-compact-layout.test.js`
- 已再次验证 `popup.js` 语法通过：`node --check /e/vscode/009_chromeExt/popup.js`
- 已再次验证 `manifest.json` 为合法 JSON：`python -m json.tool /e/vscode/009_chromeExt/manifest.json >/dev/null`
- 当前环境仍未实际打开浏览器扩展 popup，因此这一轮仍需你重点人工判断：
  - 中号 / 小号组卡的内部行间距是否已经收得够紧
  - 组头右上角总余额是否看起来有帮助，还是反而显得拥挤
  - 新 token 更新后，Ikun / 发现是否恢复正常显示

## 简短复盘（中小卡组内行距、组头总余额与 token 更新）
- 值得保留：把“主间距固定”和“组内行距单独收紧”拆成两层 token，后续微调中小卡时不容易误伤整体网格节奏。
- 可优化：组头总余额是否值得保留，必须以你在真实 popup 里的第一眼观感为准；如果你觉得嘈杂，下一轮我会直接删掉这块，而不是继续硬调。

## 新一轮：恢复组卡行距，单独收紧中小号单卡节奏
- 根据你对截图的纠正，在 `/e/vscode/009_chromeExt/popup.html` 撤销了上一轮“收紧组卡内部行距”的错误方向：
  - 中号卡与小号卡的 `--group-gap` 都恢复为 `6px`，不再继续压缩组卡内部两行成员卡之间的距离。
  - 保留组头右上角总余额试显，先只修正纵向节奏，不混改其它信息层。
- 将中号 / 小号的收紧目标改为非编组卡本身，而不是组卡：
  - 为 `.single-row` 拆出独立的 `--single-row-min-height` 与 `--single-row-padding` 变量，让单卡可以单独压缩，不再误伤 `.group-row`。
  - 中号卡新增 `--single-row-min-height: 42px`、`--single-row-padding: 5px 8px`，收紧单卡纵向节奏。
  - 小号卡进一步将 `--card-min-height` 调整为 `52px`、`--card-padding` 调整为 `5px 6px`，并新增 `--single-row-min-height: 31px`、`--single-row-padding: 2px 4px`，优先解决你指出的小卡单卡间隔过丑的问题。
- 在 `/e/vscode/009_chromeExt/task-popup-ui.test.js` 同步回改静态断言：
  - 不再锁定 `3 列 = 4px / 4 列 = 3px` 的错误组卡间距。
  - 改为锁定中号 / 小号组卡恢复 `6px` 默认行距，以及 `.single-row` 独立尺寸变量已接入。

## 验证记录（恢复组卡行距，单独收紧中小号单卡节奏）
- 已通过 popup UI 结构回归：`node /e/vscode/009_chromeExt/task-popup-ui.test.js`
- 已通过左键刷新回归：`node /e/vscode/009_chromeExt/task-click-refresh.test.js`
- 已通过显式布局回归：`node /e/vscode/009_chromeExt/task-compact-layout.test.js`
- 已通过站点 / 分组配置状态回归：`node /e/vscode/009_chromeExt/task-site-config-state.test.js`
- 已再次验证 `popup.js` 语法通过：`node --check /e/vscode/009_chromeExt/popup.js`
- 当前环境仍未实际打开浏览器扩展 popup，因此这一轮最关键的仍是你在真实 popup 里确认：
  - 中号 / 小号下，非编组卡的纵向间隔是否已经顺眼
  - 编组卡内部两行成员卡的间距是否已恢复正常
  - 组头右上角总余额是加分还是显得拥挤

## 简短复盘（恢复组卡行距，单独收紧中小号单卡节奏）
- 值得保留：先把“哪一层节奏出了问题”纠清，再拆单卡专属 token 去修，比继续挤压共享 `row` / `group` 变量更稳，不容易把组卡一起带坏。
- 可优化：这次调整已经把单卡和组卡的纵向节奏拆开了；如果你实际看下来小卡还嫌松，下一步应该优先继续微调小卡单卡的 `single-row` 与 `card`，而不是再碰组卡内部间距。

## 新一轮：卡片尺寸收口为大 / 小两档
- 在 `/e/vscode/009_chromeExt/popup.html` 删除了原来的 4 列“小号”选项与对应样式，只保留两档卡片大小：
  - `2 = 大`，每行 2 张
  - `3 = 小`，每行 3 张
- 将原先 3 列“中号”的现有视觉参数直接保留下来，改作为新的“小号”使用，不再继续维护第三套 4 列尺寸 token。
- 在 `/e/vscode/009_chromeExt/popup.js` 收口 `layoutColumns` 的合法值集合，只允许 `2 | 3`；同时按你的要求兼容旧配置：历史 `layoutColumns = 4` 会在读取时自动迁移为 `2`，避免继续保留已删除视图。
- 在 `/e/vscode/009_chromeExt/task-popup-ui.test.js` 与 `/e/vscode/009_chromeExt/task-yybb-diagnostics.test.js` 同步更新断言，锁定“两档尺寸 + 4 -> 2 迁移”这一新规则。

## 新一轮：顶部工具栏、主题切换与新增站点向导
- 在 `/e/vscode/009_chromeExt/popup.html` 重排顶部工具栏：左侧改为 `➕` 新增站点和 `⚙️` 设置，右侧只保留 `🔄` 同步。
- 将设置面板压缩为同排三项：自动排序、卡片大小、颜色模式；并新增 `colorModeToggle` 滑块。
- 为 popup 补了一套 `light` theme token，并在 `/e/vscode/009_chromeExt/popup.js` 接入 `colorMode` 的读取、应用、切换和持久化；非法值统一回退为 `dark`。
- 扩展 `siteConfigs` 状态模型，支持三类持久化记录：内置站点 override、`removed: true` 的隐藏内置站点、`isCustom: true` 的自定义 JSON 站点。
- 新增自定义站点运行时构建能力：`fieldPath` 提取 + 可选 `divideBy` 运算 + reload 后自动恢复 `extract` 函数。
- 为新增站点补齐两条路径：恢复当前未显示的内置站点；新建自定义 JSON 站点，并要求先测接口再保存。
- 自定义站点向导第一版流程：
  - 第一步填写站点名、接口地址、字段路径。
  - 点击“下一步”立即测试抓取。
  - 若失败，明确反馈 HTTP / 鉴权 / JSON / 字段缺失 / 非数字等卡点。
  - 遇到常见鉴权失败时，按需展示 `authorization` / `token` / `new-api-user` 补齐区。
  - 测试成功后进入第二步，可选填写除数并实时预览最终显示值。
  - 点击完成后写入 `siteConfigs`、立即刷新当前视图并持久化。
- 单独修正了一个恢复流程 bug：恢复某一个隐藏内置站点时，不会再误把其他已隐藏站点一起恢复。

## 验证记录（顶部工具栏、主题切换与新增站点向导）
- 已通过 popup UI 结构回归：`node /e/vscode/009_chromeExt/task-popup-ui.test.js`
- 已通过站点配置 / 主题 / 自定义站点恢复回归：`node /e/vscode/009_chromeExt/task-site-config-state.test.js`
- 已通过左键刷新回归：`node /e/vscode/009_chromeExt/task-click-refresh.test.js`
- 已通过显式布局回归：`node /e/vscode/009_chromeExt/task-compact-layout.test.js`
- 已通过 yybb / token 诊断回归：`node /e/vscode/009_chromeExt/task-yybb-diagnostics.test.js`
- 已再次验证 `popup.js` 语法通过：`node --check /e/vscode/009_chromeExt/popup.js`
- 当前环境仍未实际打开浏览器扩展 popup，因此以下项仍需你在浏览器中人工验收：
  - 顶部按钮顺序是否完全符合预期。
  - 深 / 浅色切换的实际观感。
  - 恢复内置站点和自定义 JSON 站点两条向导路径。
  - 鉴权补齐后的重试体验与 `/500000` 这类运算预览。

## 简短复盘（顶部工具栏、主题切换与新增站点向导）
- 值得保留：把“运行时显示态 siteConfigs”和“持久化原始 siteConfigs”分开维护后，像 removed 内置站点、custom 站点恢复这类状态不会再互相污染。
- 可优化：当前新增站点向导已经闭环，但还缺真实浏览器中的人工走查；下一步最重要的是实际点一遍流程，把文案、焦点和输入尺寸再按观感收一轮。

